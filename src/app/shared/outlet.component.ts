import { Component } from '@angular/core';

@Component({
  selector: 'tb-outlet',
  template: `<router-outlet name="xyz"></router-outlet>`
})
export class OutletComponent {
  // private name: string = 'test';
}
